// Config starter code
import { createChatBotMessage } from "react-chatbot-kit";

const config = {
  initialMessages: [
    createChatBotMessage(`I am your beta reader. Ask me anything`, {}),
  ],
};

export default config;
